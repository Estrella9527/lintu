/// <reference types="vite/client" />

// Side-effect CSS imports (e.g. `import './index.css'`) — the bundler handles
// the actual loading; this declaration just tells tsc the import is valid.
declare module '*.css'
