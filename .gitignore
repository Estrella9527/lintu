node_modules/
dist/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
*.db
bun.lock
.DS_Store
lintu-data/

# Build artifacts
release/
apps/electron/release/
apps/sidecar/dist/
apps/sidecar/build/
.pytest_cache/
.ruff_cache/

# Code signing material — NEVER commit private keys
apps/electron/build/codesign.pfx
apps/electron/build/codesign.cer
apps/electron/build/*.pfx
apps/electron/build/*.p12

# Backup system credentials — PG 密码 / 加密 key / OSS 凭证
scripts/backup/.env

# Internal docs with real infra values (IP / domain / customer names) — never push
docs/internal/
